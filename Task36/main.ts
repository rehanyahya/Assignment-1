const make_shirt = (message: string, size: string) => {
  console.log(
    `The size of shirt is ${size} and the message printed on shirt is \'${message}\'`
  );
};

make_shirt("This is my shirt", "medium");
